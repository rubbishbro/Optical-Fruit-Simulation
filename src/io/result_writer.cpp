#include "fruitsim/io/result_writer.hpp"
#include "fruitsim/geometry/mesh_geometry.hpp"

#include <fstream>
#include <iomanip>
#include <nlohmann/json.hpp>
#include <numeric>
#include <stdexcept>

namespace fruitsim {

namespace {

void write_mesh_ply(const std::filesystem::path& path, const StatisticalShapeMesh& mesh)
{
    std::ofstream stream(path);
    if (!stream) throw std::runtime_error("Cannot create mesh file: " + path.string());
    stream << "ply\nformat ascii 1.0\n"
           << "comment generated by fruitsim CPU mesh transport\n"
           << "element vertex " << mesh.vertices_mm.size() << "\n"
           << "property double x\nproperty double y\nproperty double z\n"
           << "element face " << mesh.faces.size() << "\n"
           << "property list uchar int vertex_indices\nend_header\n";
    stream << std::setprecision(12);
    for (const auto& vertex : mesh.vertices_mm) {
        stream << vertex.x() << ' ' << vertex.y() << ' ' << vertex.z() << '\n';
    }
    for (const auto& face : mesh.faces) {
        stream << "3 " << face[0] << ' ' << face[1] << ' ' << face[2] << '\n';
    }
}

std::ofstream output_file(const std::filesystem::path& path)
{
    std::ofstream stream(path);
    if (!stream) {
        throw std::runtime_error("Cannot create output file: " + path.string());
    }
    stream << std::setprecision(12);
    return stream;
}

} // namespace

void write_simulation_results(
    const SimulationProblem& problem, const SimulationResult& result,
    const std::filesystem::path& output_directory)
{
    std::filesystem::create_directories(output_directory);

    if (problem.mesh_geometry) {
        write_mesh_ply(output_directory / "outer_mesh.ply", problem.mesh_geometry->outer_mesh());
        write_mesh_ply(output_directory / "inner_mesh.ply", problem.mesh_geometry->inner_mesh());
    }

    auto summary = output_file(output_directory / "summary.csv");
    summary << "wavelength_nm,photons,reflectance,reflectance_se,transmittance,"
               "transmittance_se,absorbed_total,discarded_weight,boundary_failures,"
               "max_event_terminations,energy_residual,"
               "penetration_q50_mm,penetration_q90_mm,launched_photons,"
               "detected_photon_count,detected_weight,detection_efficiency,"
               "detected_specular_weight,detected_diffuse_weight,"
               "detected_reflectance,detected_penetration_mean_mm,"
               "detected_penetration_median_mm,weighted_mean_total_path_mm,"
               "weighted_mean_skin_path_mm,weighted_mean_flesh_path_mm,"
               "skin_path_fraction,flesh_path_fraction";
    for (const auto& layer : problem.domain.layers()) summary << ",absorbed_" << layer.name;
    summary << '\n';
    for (const auto& wavelength : result.wavelengths) {
        const double absorbed = std::accumulate(
            wavelength.absorbed_by_region.begin(), wavelength.absorbed_by_region.end(), 0.0);
        summary << wavelength.wavelength_nm << ',' << wavelength.photons << ','
                << wavelength.reflectance << ',' << wavelength.reflectance_standard_error << ','
                << wavelength.transmittance << ',' << wavelength.transmittance_standard_error << ','
                << absorbed << ',' << wavelength.discarded_weight << ','
                << wavelength.boundary_failures << ',' << wavelength.max_event_terminations << ','
                << wavelength.energy_residual << ',' << wavelength.penetration_q50_mm << ','
                << wavelength.penetration_q90_mm << ',' << wavelength.photons << ','
                << wavelength.detected_photon_count << ',' << wavelength.detected_weight << ','
                << wavelength.detection_efficiency << ','
                << wavelength.detected_specular_weight << ','
                << wavelength.detected_diffuse_weight << ','
                << wavelength.detected_reflectance << ','
                << wavelength.detected_penetration_mean_mm << ','
                << wavelength.detected_penetration_median_mm << ','
                << wavelength.weighted_mean_total_path_mm << ','
                << wavelength.weighted_mean_skin_path_mm << ','
                << wavelength.weighted_mean_flesh_path_mm << ','
                << wavelength.skin_path_fraction << ',' << wavelength.flesh_path_fraction;
        for (double value : wavelength.absorbed_by_region) summary << ',' << value;
        summary << '\n';
    }

    auto detectors = output_file(output_directory / "detectors.csv");
    detectors << "wavelength_nm,detector,bin_index,r_min_mm,r_max_mm,value\n";
    for (const auto& wavelength : result.wavelengths) {
        for (std::size_t index = 0; index < wavelength.radial_reflectance.size(); ++index) {
            const double width = problem.scoring.radial_max_mm
                / static_cast<double>(problem.scoring.radial_bins);
            detectors << wavelength.wavelength_nm << ",reflectance_radial," << index << ','
                      << index * width << ',' << (index + 1) * width << ','
                      << wavelength.radial_reflectance[index] << '\n';
        }
    }

    auto instrument = output_file(output_directory / "instrument.csv");
    instrument << "wavelength_nm,launched_photons,detected_photon_count,detected_weight,"
                  "detected_specular_weight,detected_diffuse_weight,"
                  "detection_efficiency,detected_reflectance,"
                  "detected_penetration_mean_mm,detected_penetration_median_mm,"
                  "weighted_mean_total_path_mm,weighted_mean_skin_path_mm,"
                  "weighted_mean_flesh_path_mm,skin_path_fraction,flesh_path_fraction\n";
    for (const auto& wavelength : result.wavelengths) {
        instrument << wavelength.wavelength_nm << ',' << wavelength.photons << ','
                   << wavelength.detected_photon_count << ',' << wavelength.detected_weight << ','
                   << wavelength.detected_specular_weight << ','
                   << wavelength.detected_diffuse_weight << ','
                   << wavelength.detection_efficiency << ',' << wavelength.detected_reflectance << ','
                   << wavelength.detected_penetration_mean_mm << ','
                   << wavelength.detected_penetration_median_mm << ','
                   << wavelength.weighted_mean_total_path_mm << ','
                   << wavelength.weighted_mean_skin_path_mm << ','
                   << wavelength.weighted_mean_flesh_path_mm << ','
                   << wavelength.skin_path_fraction << ',' << wavelength.flesh_path_fraction << '\n';
    }

    auto instrument_regions = output_file(output_directory / "instrument_regions.csv");
    instrument_regions << "wavelength_nm,region_index,region_name,"
                          "weighted_mean_path_mm,path_fraction\n";
    for (const auto& wavelength : result.wavelengths) {
        for (std::size_t region = 0; region < problem.domain.layers().size(); ++region) {
            instrument_regions << wavelength.wavelength_nm << ',' << region << ','
                << problem.domain.layers()[region].name << ','
                << wavelength.weighted_mean_path_by_region_mm.at(region) << ','
                << wavelength.path_fraction_by_region.at(region) << '\n';
        }
    }

    const std::uint64_t total_photons = std::accumulate(result.wavelengths.begin(),
        result.wavelengths.end(), std::uint64_t{0},
        [](std::uint64_t total, const WavelengthResult& wavelength) {
            return total + wavelength.photons;
        });
    const double photons_per_second = result.elapsed_seconds > 0.0
        ? static_cast<double>(total_photons) / result.elapsed_seconds : 0.0;
    auto performance = output_file(output_directory / "performance.csv");
    performance << "backend,wavelength_count,photons_per_wavelength,total_photons,"
                   "elapsed_seconds,photons_per_second\n"
                << result.backend << ',' << result.wavelengths.size() << ','
                << problem.execution.photons_per_wavelength << ',' << total_photons << ','
                << result.elapsed_seconds << ',' << photons_per_second << '\n';

    if (problem.scoring.grid_size > 0) {
        auto grid = output_file(output_directory / "absorption_grid.csv");
        grid << "wavelength_nm,x_index,y_index,z_index,absorbed_weight\n";
        const std::size_t size = problem.scoring.grid_size;
        for (const auto& wavelength : result.wavelengths) {
            for (std::size_t z = 0; z < size; ++z) {
                for (std::size_t y = 0; y < size; ++y) {
                    for (std::size_t x = 0; x < size; ++x) {
                        const std::size_t index = (z * size + y) * size + x;
                        const double value = wavelength.absorption_grid[index];
                        if (value != 0.0) {
                            grid << wavelength.wavelength_nm << ',' << x << ',' << y << ','
                                 << z << ',' << value << '\n';
                        }
                    }
                }
            }
        }
    }

    if (problem.scoring.trajectory_limit > 0
        && problem.scoring.trajectory_mode != "detector_only") {
        auto trajectories = output_file(output_directory / "trajectories.csv");
        trajectories << "wavelength_nm,photon_id,event,x_mm,y_mm,z_mm,weight,region,"
                        "detector_accepted\n";
        for (const auto& wavelength : result.wavelengths) {
            for (const auto& point : wavelength.trajectories) {
                trajectories << wavelength.wavelength_nm << ',' << point.photon_id << ','
                             << point.event << ',' << point.position_mm.x() << ','
                             << point.position_mm.y() << ',' << point.position_mm.z() << ','
                             << point.weight << ',' << point.region << ','
                             << (point.detector_accepted ? 1 : 0) << '\n';
            }
        }
    }

    if (problem.scoring.detector_trajectory_limit > 0) {
        auto detector_rays = output_file(output_directory / "detector_rays.csv");
        detector_rays << "wavelength_nm,photon_id,event,x_mm,y_mm,z_mm,weight,region,"
                         "detector_accepted\n";
        for (const auto& wavelength : result.wavelengths) {
            for (const auto& trace : wavelength.detector_trajectories) {
                for (const auto& point : trace) {
                    detector_rays << wavelength.wavelength_nm << ',' << point.photon_id << ','
                                  << point.event << ',' << point.position_mm.x() << ','
                                  << point.position_mm.y() << ',' << point.position_mm.z() << ','
                                  << point.weight << ',' << point.region << ','
                                  << (point.detector_accepted ? 1 : 0) << '\n';
                }
            }
        }
    }

    nlohmann::json manifest{
        {"schema_version", 1},
        {"software", "fruitsim"},
        {"software_version", "0.5.0"},
        {"session_id", problem.metadata.session_id},
        {"cultivar", problem.metadata.cultivar},
        {"dataset_id", problem.metadata.dataset_id},
        {"source_type", problem.metadata.source_type},
        {"transport_mode", problem.metadata.transport_mode},
        {"assumptions", problem.metadata.assumptions},
        {"backend", result.backend},
        {"runtime_metadata", result.runtime_metadata},
        {"seed", problem.execution.seed},
        {"threads", problem.execution.threads},
        {"photons_per_wavelength", problem.execution.photons_per_wavelength},
        {"elapsed_seconds", result.elapsed_seconds},
        {"wavelength_count", result.wavelengths.size()},
        {"total_photons", total_photons},
        {"photons_per_second", photons_per_second},
        {"trajectory_mode", problem.scoring.trajectory_mode},
        {"trajectory_limit", problem.scoring.trajectory_limit},
        {"detector_trajectory_limit", problem.scoring.detector_trajectory_limit},
        {"detector_trajectory_sampling", "reservoir_sampling_after_full_transport"},
        {"instrument_assumption_status",
         problem.detector.enabled ? "simulation_demo_assumption" : "detector_disabled"},
        {"source", {
            {"type", problem.source.type},
            {"center_or_position_mm", {problem.source.position_mm.x(),
                problem.source.position_mm.y(), problem.source.position_mm.z()}},
            {"direction", {problem.source.direction.x(), problem.source.direction.y(),
                problem.source.direction.z()}},
            {"ring_plane_normal", {problem.source.ring_plane_normal.x(),
                problem.source.ring_plane_normal.y(), problem.source.ring_plane_normal.z()}},
            {"ring_radius_mm", problem.source.ring_radius_mm},
            {"ring_width_mm", problem.source.ring_width_mm},
            {"direction_mode", problem.source.direction_mode},
            {"target_mm", {problem.source.target_mm.x(), problem.source.target_mm.y(),
                problem.source.target_mm.z()}},
            {"spatial_sampling", problem.source.spatial_sampling},
        }},
        {"detector", {
            {"enabled", problem.detector.enabled},
            {"type", problem.detector.type},
            {"center_mm", {problem.detector.center_mm.x(), problem.detector.center_mm.y(),
                problem.detector.center_mm.z()}},
            {"axis_toward_sample", {problem.detector.axis.x(), problem.detector.axis.y(),
                problem.detector.axis.z()}},
            {"radius_mm", problem.detector.radius_mm},
            {"surface_model", "analytic_circular_disk"},
            {"surface_diameter_mm", 2.0 * problem.detector.radius_mm},
            {"acceptance_half_angle_deg", problem.detector.acceptance_half_angle_deg},
            {"numerical_aperture", problem.detector.numerical_aperture},
            {"numerical_aperture_medium_index", problem.exterior_refractive_index},
        }},
        {"detector_metric_definition",
         "detection_efficiency = detected_weight / launched_photons; detector scoring is an "
         "observation of escaped weight and is not subtracted from R/T/A; detected_weight = "
         "detected_specular_weight + detected_diffuse_weight"},
        {"penetration_depth_definition",
         "maximum along-path geometric inward depth: outer_radius - distance(point, center)"},
        {"detected_path_definition",
         "mean region and total path lengths are weighted by detector-arrival packet weight; "
         "fractions divide each weighted region-path sum by the weighted total-path sum"},
        {"synthetic_warning",
         problem.metadata.source_type == "synthetic"
             ? "METHOD DEMONSTRATION ONLY - NOT VALID FOR REAL APPLE SSC PREDICTION"
             : ""},
    };
    auto manifest_stream = output_file(output_directory / "manifest.json");
    manifest_stream << manifest.dump(2) << '\n';
}

} // namespace fruitsim
